# Core workspace guidance

Preserve the independent Core package build and exact-ref TreeDX custody.

## Project library

Use `trsd library show core` and `status` before querying `treeseed-ai/core-library`. Read root-level paths at an exact commit. Author only through governed library workspaces and reviews. Never recreate `src/content` or edit `.treeseed/data` directly.
